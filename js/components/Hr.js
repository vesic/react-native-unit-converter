import React from 'react'
import { View } from 'react-native'

const Hr = ({}) => (
  <View
    style={{
      borderBottomColor: 'black',
      borderBottomWidth: 1,
    }}
  />
)

export default Hr
